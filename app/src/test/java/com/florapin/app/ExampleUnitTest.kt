package com.florapin.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test unitaire local (JVM) — sert surtout à valider la chaîne de build/test.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
